import Milestones from "../components/Milestones"; // Keep the imported name as is

const MilestonesWrapper = () => { // Rename the local component
  return (
    <div>
      <Milestones />
    </div>
  );
};

export default MilestonesWrapper; // Export the renamed component